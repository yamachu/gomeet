{
  "name": "slack-google-oauth-callback-handler",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  },
  "dependencies": {
    "@server/core": "file:../../core"
  }
}
